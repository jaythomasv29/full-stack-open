sequenceDiagram
participant browser
participant server

    Note right of browser: User enters a new note and clicks Save

    Note right of browser: JavaScript handles the submit event and prevents the page from reloading using event.preventDefault()

    Note right of browser: A new note object is created and added to the local notes array via .push()

    Note right of browser: JavaScript redraws/re-renders the notes on the page, not an entire page load

    browser->>server: POST /exampleapp/new_note_spa<br/>{"content":"new note","date":"..."}
    activate server

    server-->>browser: 201 Created
    deactivate server

    Note right of browser: No redirect or additional GET requests are needed
